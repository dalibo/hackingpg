/*-------------------------------------------------------------------------
 * cpg.c: A Corosync background worker for PostgreSQL
 *
 * Its goal is to detect which node is the primary and dynamically set the
 * primary conninfo in accordance.
 *
 * Interacting with systemd, it is also able to tirgger a dumb and fearless
 * switchover, without any safety belt.
 *
 * Base code inspired by src/test/modules/worker_spi/worker_spi.c
 * Documentation: https://www.postgresql.org/docs/current/bgworker.html
 *
 * This program is open source, licensed under the PostgreSQL license.
 * For license terms, see the LICENSE file.
 *
 * Copyright (c) 2023-2024, Jehan-Guillaume de Rorthais
 *-------------------------------------------------------------------------
 */

#include <limits.h>					// INT_MAX
#include <corosync/cpg.h>			// corosync funcs

#include "postgres.h"				// definitions useful for PostgreSQL procs
#include "postmaster/bgworker.h"	// for a bgworker
#include "miscadmin.h"				// various variables, macros and funcs
#include "fmgr.h"					// needed for PG_MODULE_MAGIC
#include "storage/latch.h"			// for *Latch()
#include "utils/wait_event.h"		// for *Latch()
#include "utils/guc.h"				// for GUC
#include "postmaster/interrupt.h"	// for HandleMainLoopInterrupts() & co
#include "utils/ps_status.h"		// rename process
#include "access/xlog.h"			// for RecoveryInProgress()
#include "storage/fd.h"				// for BasicOpenFile
#include "executor/spi.h"			// for SPI funcs
#include "utils/snapmgr.h"			// for xact control
#include "pgstat.h"					// for pgstat_* funcs

#define CPG_GROUP_NAME "pgsql_group"
/* Name of the file where the module write the primary_conninfo setting */
#define CPG_AUTOCONF_FILENAME "cpg.auto.conf"

/* Required in all loadable module */
PG_MODULE_MAGIC;

/*
 * Declare main function of the bgworker and make sure it is callable from
 * external code using "PGDLLEXPORT"
 */
PGDLLEXPORT void cpg_main(Datum main_arg);

/*
 * Global variables
 */

/* Maximum interval between bgworker wakeups */
static int interval;
/* Remember recovery state */
static bool in_recovery;
/* Remember the number of known members */
static uint32_t members;
/* Local node id as set in corosync config */
static uint32_t MyNodeID;
/* Local hostname */
static char hostname[HOST_NAME_MAX];
/* Known primary */
static char current_primary[HOST_NAME_MAX];
/* Structure holding the group handle */
static cpg_handle_t gh;
/* Group name in corosync internal format */
static struct cpg_name cpg_group;
/* flag shutdown set by signal handlers */
static volatile sig_atomic_t pending_shutdown = false;
/* flag switchover set by signal handlers */
static volatile sig_atomic_t pending_switchover = false;

/*
 * Functions
 */

/*
 * Set primary_conninfo GUC and ask postmaster to reload. Take the value of
 * the parameter.
 */
static void
set_primary_conninfo(char * primary_conninfo)
{

	StringInfoData	buf;
	int				fd;

	fd = BasicOpenFile(CPG_AUTOCONF_FILENAME, O_CREAT | O_RDWR | O_TRUNC);

	initStringInfo(&buf);

	appendStringInfo(&buf,
					 "# Do not edit this file manually!\n"
					 "# It is handled by the cpg module.\n"
					 "primary_conninfo = '%s'\n",
					 primary_conninfo);

	errno = 0;
	if (write(fd, buf.data, buf.len) != buf.len)
	{
		/* if write didn't set errno, assume problem is no disk space */
		if (errno == 0) errno = ENOSPC;
		ereport(ERROR,
				(errcode_for_file_access(),
				 errmsg("could not write to file \"%s\": %m", CPG_AUTOCONF_FILENAME)));
	}

	close(fd);
	pfree(buf.data);
	/* We can safetly lose this file if a crash occurs, no need to fsync */

	/* Ask postmatser to reload the configuration */
	if(kill(PostmasterPid, SIGHUP))
		elog(FATAL,
			 "[cpg] cs_message_cb: failed to send signal to postmaster");
}

/*
 * Update processus title
 */
static void
update_ps_display()
{
	char ps_display[128];

	snprintf(ps_display, 128, "[%u] primary: %s", members, current_primary);

	set_ps_display(ps_display);
}

/*
 * Signal handler for SIGTERM
 *
 * Say good bye and leave the group gracefuly.
 */
static void
cpg_sigterm(SIGNAL_ARGS)
{
	elog(LOG, "[cpg] pending shutdown…");
	pending_shutdown = true;
	/* Trigger the latch to wake up the event loop immediately*/
	SetLatch(MyLatch);
}

/*
 * Signal handler for SIGUSR2, switchover.
 */
static void
cpg_sigusr2(SIGNAL_ARGS)
{
	elog(LOG, "[cpg] Signaled to switchover");
	pending_switchover = true;
	SetLatch(MyLatch);
}

/*
 * Send the given message "data" of length "len" using the group handle "gh"
 */
static void
send_msg(cpg_handle_t gh, char *data, int len)
{
	struct iovec iovec;
	int rc;

	/* Setup the structure holding the message */
	iovec.iov_base = (void *)data;
	iovec.iov_len = len;

	/* Send the message */
	rc = cpg_mcast_joined(gh, CPG_TYPE_FIFO, &iovec, 1);
	if (rc == CS_OK)
		/*
		 * For this demo, we expect message to be text only, so let's log it
		 * for better observability.
		 */
		elog(DEBUG1, "[cpg] message sent: %s", data);
	else
		elog(FATAL, "[cpg] could not send message: %d", rc);
}

/*
 * Leave the group gracefuly, reset primary_conninfo and shutdown.
 */
static void
cpg_shutdown()
{
	cs_error_t rc;

	/* Reset shutdown signal */
	pending_shutdown = false;

	elog(LOG, "[cpg] …and leaving");

	/* Leave the closed group */
	rc = cpg_leave(gh, &cpg_group);
	if ( rc == CS_OK )
		elog(LOG, "[cpg] left group '" CPG_GROUP_NAME "'");
	else
		elog(FATAL, "[cpg] leaving group '" CPG_GROUP_NAME "' failed: %d", rc);

	/* Deallocate the group handler */
	rc = cpg_finalize(gh);
	if ( rc == CS_OK )
		elog(DEBUG1, "[cpg] handle finalized");
	else
		elog(FATAL, "[cpg] finalizing handle failed: %d", rc);

	/* Remove any primary_conninfo setting */
	set_primary_conninfo("");

	/* Shutdown */
	exit(0);
}

/*
 * Trigger a smart promote on a remote node and shutdown gracefuly.
 */
static void
cpg_switchover()
{
	int i, fd;
	uint32_t candidate;
	char msg[128];

	/* Corosync vars */
	struct cpg_address addresses[2];
	cs_error_t rc;
	int num_addresses;

	/* Reset switchover signal */
	pending_switchover = false;

	if (in_recovery)
	{
		elog(LOG, "[cpg] Ignoring switchover signal: already a standby.");
		return;
	}

	/* Get at least two group members to find a candidate not being ourself */
	num_addresses = 2;
	rc = cpg_membership_get(gh, &cpg_group, addresses, &num_addresses);
	if (rc != CS_OK)
		elog(FATAL, "[cpg] could not get the group members: %d", rc);

	for (i = 0; i < 2; i++)
	{
		if (addresses[i].nodeid != MyNodeID)
		{
			candidate = addresses[i].nodeid;
			break;
		}
	}

	elog(LOG, "[cpg] Initiating switchover to node %u", candidate);

	snprintf(msg, 128, "promote=%u", candidate);
	send_msg(gh, msg, strlen(msg) + 1);

	/* Create standby.signal file */
	fd = BasicOpenFile(STANDBY_SIGNAL_FILE, O_CREAT | O_RDWR | O_TRUNC);
	close(fd);

	/* Shutdown ourself */
	kill(PostmasterPid, SIGTERM);
}

/*
 * Smart self-promotion, waiting for replication to finish first.
 */
static void
cpg_promote()
{
	int rc;
	int is_streaming;
	bool isnull;
	char *promote_query = "SELECT pg_promote()";
	char *wal_receiver_query = "SELECT count(*) "
							   "FROM pg_catalog.pg_stat_wal_receiver";

	elog(DEBUG1, "[cpg] Received a promotion signal");

	/* We must set the current timestamp before creating a xact so it is
	 * timestamped and its first query as well */
	SetCurrentStatementStartTimestamp();

	/* Start a xact, this must be done before any SPI call:
	 * https://www.postgresql.org/message-id/1003166.1627657587%40sss.pgh.pa.us
	 */
	StartTransactionCommand();

	/* Setup an SPI environment, its memory context, globals init, etc. */
	SPI_connect();

	/* Required to have a MVCC snapshot */
	PushActiveSnapshot(GetTransactionSnapshot());

	/* Waiting for the replication to end */
	do
	{
		elog(DEBUG1, "[cpg] Waiting for streaming to finish…");

		/* pausing 1/10th of sec between each check */
		WaitLatch(NULL, WL_EXIT_ON_PM_DEATH | WL_TIMEOUT, 100,
				  PG_WAIT_EXTENSION);

		/* Set the current timestamp for next query start time */
		SetCurrentStatementStartTimestamp();
		/* Report our activity */
		pgstat_report_activity(STATE_RUNNING, wal_receiver_query);

		rc = SPI_execute(wal_receiver_query, true, 1);
		if (rc != SPI_OK_SELECT || SPI_processed != 1)
			elog(FATAL, "Could not query the wal receiver state: %d", rc);

		is_streaming = DatumGetInt64(SPI_getbinval(SPI_tuptable->vals[0],
									 SPI_tuptable->tupdesc,
									 1, &isnull));

		/* Report our inactivity */
		pgstat_report_activity(STATE_IDLE, NULL);
	}
	while(is_streaming > 0);

	elog(DEBUG1, "[cpg] Replication done");

	SetCurrentStatementStartTimestamp();
	pgstat_report_activity(STATE_RUNNING, promote_query);
	rc = SPI_execute(promote_query, true, 0);
	if (rc != SPI_OK_SELECT)
		elog(FATAL, "Promotion query failed: %d", rc);
	pgstat_report_activity(STATE_IDLE, NULL);

	SPI_finish();
	PopActiveSnapshot();
	CommitTransactionCommand();

	/* Report the bgwork activity stat (mainly functions call) */
	pgstat_report_stat(false);
}

/*
 * Corosync callbacks. See cpg_initialize(3) for reference.
 *
 */

/*
 * Implementation of the CPG config callback. Called when members join or
 * leave the group.
 */
static void
cs_config_cb(cpg_handle_t gh,
			 const struct cpg_name *groupName,
			 const struct cpg_address *member_list,
			 size_t member_list_entries,
			 const struct cpg_address *left_list,
			 size_t left_list_entries,
			 const struct cpg_address *joined_list,
			 size_t joined_list_entries)
{
	unsigned int i;
	StringInfoData msg;

	/* Update number of members in the process title */
	members = member_list_entries;
	update_ps_display();

	/* Sum up current members */
	initStringInfo(&msg);
	for (i = 0; i < member_list_entries; i++)
		appendStringInfo(&msg, ", %d/%d", member_list[i].nodeid,
						 member_list[i].pid);

	elog(LOG, "[cpg] %lu join, %lu left, procs in group now: %s",
		 joined_list_entries, left_list_entries, msg.data+2);

	/* Loop on newcomers to the group */
	for (i = 0; i < joined_list_entries; i++)
	{
		if (joined_list[i].nodeid == MyNodeID
			&& (pid_t)joined_list[i].pid == MyProcPid)
		{
			/* The local node is now confirmed in the group */
			resetStringInfo(&msg);

			gethostname(hostname, HOST_NAME_MAX);

			appendStringInfo(&msg, "Hi, I'm %s!", hostname);

			/* Introduce the local node to other ones… */
			send_msg(gh, msg.data, msg.len + 1);

			/* …and ask who is the primary if needed */
			resetStringInfo(&msg);
			if (RecoveryInProgress())
				appendStringInfoString(&msg, "primary?");
			else
			{
				/* Tell everyone the local node is a primary */
				appendStringInfo(&msg, "primary=%s", hostname);

				/* set current primary, will be updated in process title soon */
				strncpy(current_primary, "myself!", HOST_NAME_MAX);
			}

			send_msg(gh, msg.data, msg.len + 1);
		}
	}

	pfree(msg.data);

	/* Did I left the group ? */
	if (left_list_entries
		&& (pid_t)left_list[0].pid == MyProcPid
		&& left_list[0].nodeid == MyNodeID)
	{
		/* …then quit */
		elog(FATAL, "[cpg] I left the closed process group!");
	}
}

/*
 * Implementation of the CPG message callback.
 * Called when a message is received, even our own ones!
 */
static void
cs_message_cb(cpg_handle_t gh, const struct cpg_name *groupName,
			  uint32_t nodeid, uint32_t pid, void *msg, size_t msg_len)
{
	char nodename[HOST_NAME_MAX];
	uint32_t to_promote;

	if (nodeid == MyNodeID && pid == MyProcPid)
		elog(DEBUG1, "[cpg] ignoring own message");
	else
	{
		elog(LOG, "[cpg] (%d/%d): «%s»", nodeid, pid, (const char *) msg);

		/* Answering primary request */
		if (strncmp((const char *) msg, "primary?", 9) == 0
			&& !RecoveryInProgress())
		{
			StringInfoData msg;

			initStringInfo(&msg);
			appendStringInfo(&msg, "primary=%s", hostname);

			send_msg(gh, msg.data, msg.len + 1);

			strncpy(current_primary, "myself!", HOST_NAME_MAX);
			update_ps_display();

			pfree(msg.data);
		}
		/* Receiving primary answer */
		else if (sscanf((const char *) msg, "primary=%s", nodename) == 1)
		{
			/* just enough to hold "host=" plus the hostname */
			char conninfo[HOST_NAME_MAX + 5];

			if (strncmp(current_primary, nodename, HOST_NAME_MAX) == 0)
			{
				elog(DEBUG1, "[cpg] already known primary: %s", nodename);
				return;
			}

			elog(LOG, "[cpg] the primary answered: %s", nodename);

			/* build the trivial conninfo */
			strcpy(conninfo, "host=");
			strncpy(conninfo+5, nodename, HOST_NAME_MAX);

			/* connect to the new primary! */
			set_primary_conninfo(conninfo);

			/* remember the current primary */
			strncpy(current_primary, nodename, HOST_NAME_MAX);
			update_ps_display();
		}
		/* Receiving a promotion instruction */
		else if (sscanf((const char *) msg, "promote=%u", &to_promote) == 1)
		{
			if (to_promote == MyNodeID)
				cpg_promote();
		}
	}
}

/*
 * The bgworker main function.
 *
 * This is called from the bgworker backend freshly forked and setup by
 * postmaster.
 */
void
cpg_main(Datum main_arg)
{
	cs_error_t rc;
	/* Structure holding the CPG setup and callback */
	cpg_model_v1_data_t model_data;
	int32_t cpg_fd;

	/*
	 * By default, signals are blocked when the background worker starts.
	 * This allows to set up some signal handlers during the worker
	 * startup.
	 */

	/*
	 * Install signal handlers
	 */
	/* Leave gracefully on shutdown */
	pqsignal(SIGTERM, cpg_sigterm);
	/* Install signal handler to leave trigger a switchover */
	pqsignal(SIGUSR2, cpg_sigusr2);
	/* Core facility to handle config reload */
	pqsignal(SIGHUP, SignalHandlerForConfigReload);

	/* We can now unblock signals */
	BackgroundWorkerUnblockSignals();

	/*
	 * GUC declarations
	 */

	/* interval */
	DefineCustomIntVariable("cpg.interval",	// name
							"Defines the maximal interval in seconds between "
							"wakeups",		// description
							NULL,			// long description
							&interval,		// variable to set
							10,				// default value
							1,				// min value
							INT_MAX / 1000,	// max value
							PGC_SIGHUP,		// context
							GUC_UNIT_S,		// flags
							NULL,			// check hook
							NULL,			// assign hook
							NULL);			// show hook

	/*
	 * Lock namespace "cpg". Forbid creating any other GUC after here. Useful
	 * to remove unknown or mispelled GUCs from this domain.
	 */
#if PG_VERSION_NUM >= 150000
	MarkGUCPrefixReserved("cpg");
#else
	/* Note that this still exists in v15+ as a macro calling
	 * MarkGUCPrefixReserved */
	EmitWarningsOnPlaceholders("cpg");
#endif

	elog(LOG, "[cpg] Starting…");

	/* Initialize some gloabls */
	*current_primary = '\0';
	gethostname(hostname, HOST_NAME_MAX);

	/* Set initial status and proc title (in this order) */
	in_recovery = RecoveryInProgress();

	if (!in_recovery)
		strncpy(current_primary, "myself!", HOST_NAME_MAX);

	update_ps_display();

	/*
	 * Corosync initialization and membership
	 */

	/* Corosync callbacks setup */
	model_data.flags = CPG_MODEL_V1_DELIVER_INITIAL_TOTEM_CONF;
	model_data.cpg_deliver_fn = cs_message_cb;
	model_data.cpg_confchg_fn = cs_config_cb;
	/* We choose to not implement the totem callback as it is not relevent */
	model_data.cpg_totem_confchg_fn = NULL;

	/* Initialize the connection */
	rc = cpg_model_initialize(&gh, CPG_MODEL_V1,
							  (cpg_model_data_t *) &model_data, NULL);
	if (rc != CS_OK)
		elog(FATAL, "[cpg] could not init the cpg handle: %d", rc);

	/* Initialize the group name structure */
	strncpy(cpg_group.value, CPG_GROUP_NAME, CPG_MAX_NAME_LENGTH);
	cpg_group.length = strlen(CPG_GROUP_NAME);

	/* Try to join the Close Process Group! */
	rc = cpg_join(gh, &cpg_group);
	if (rc == CS_OK)
		elog(LOG, "[cpg] joined group '" CPG_GROUP_NAME);
	else if (rc == CS_ERR_INVALID_PARAM)
		elog(FATAL, "[cpg] the handle is already joined to a group");
	else
		elog(FATAL, "[cpg] could not join the close process group: %d", rc);

	/* Get the local node id. This is needed to identify our own messages */
	rc = cpg_local_get(gh, &MyNodeID);
	if (rc != CS_OK)
		elog(FATAL, "[cpg] failed to get local nodeid: %d", rc);

	/* Get the file descriptor used for the group communication. We watch it in
	 * the event loop to detect incoming events. */
	rc = cpg_fd_get(gh, &cpg_fd);
	if (rc != CS_OK)
		elog(FATAL, "[cpg] failed to get the CPG file descriptor: %d", rc);

	/* Connect to database */
	BackgroundWorkerInitializeConnection("postgres", NULL, 0);

	/* Event loop */
	for (;;)
	{
		CHECK_FOR_INTERRUPTS();

		/* Check if an event is waiting to be processed, without blocking the
		 * loop */
		rc = cpg_dispatch(gh, CS_DISPATCH_ONE_NONBLOCKING);
		if (rc == CS_OK)
			elog(DEBUG1, "[cpg] dispatched one event");
		else if (rc != CS_ERR_TRY_AGAIN)
			/* Using CS_DISPATCH_ONE_NONBLOCKING, cpg_dispatch returns
			 * CS_ERR_TRY_AGAIN when no event are waiting to be processed. This
			 * is not a failure to catch */
			elog(FATAL, "[cpg] dispatching callback failed: %d", rc);

		if (RecoveryInProgress() != in_recovery)
		{
			char msg[HOST_NAME_MAX+9];

			elog(LOG, "[cpg] I've been promoted!");

			strcpy(msg, "primary=");
			strncpy(msg+8, hostname, HOST_NAME_MAX);

			send_msg(gh, msg, 9+strlen(hostname));

			strncpy(current_primary, "myself!", HOST_NAME_MAX);

			in_recovery = RecoveryInProgress();
			update_ps_display();
		}
		else
			elog(LOG, "[cpg] Hi!");

		/* Facility from core. checks if some smiple and common interrupts,
		 * including SIGHUP to reload conf files, and process them. */
		HandleMainLoopInterrupts();

		if (pending_shutdown)
			cpg_shutdown();

		if (pending_switchover)
			cpg_switchover();

		/* Wait for an event or timeout */
		WaitLatchOrSocket(MyLatch,
						  WL_LATCH_SET | WL_TIMEOUT | WL_EXIT_ON_PM_DEATH |
						  WL_SOCKET_READABLE,
						  cpg_fd,
						  interval*1000, /* convert to milliseconds */
						  PG_WAIT_EXTENSION);
		ResetLatch(MyLatch);
	}

	elog(LOG, "[cpg] oops, the event loop broke");

	exit(1);
}

/*
 * Module init.
 *
 * This is called from the Postmaster process.
 */
void
_PG_init(void)
{
	BackgroundWorker worker;

	/*
	 * This bgw can only be loaded from shared_preload_libraries.
	 */
	if (!process_shared_preload_libraries_in_progress)
		return;

	/*
	 * Setup the bgworker
	 */

	/* Memory init */
	memset(&worker, 0, sizeof(worker));

	/* We need shmem access and database connection */
	worker.bgw_flags = BGWORKER_SHMEM_ACCESS |
					   BGWORKER_BACKEND_DATABASE_CONNECTION;

	/* Start the bgworker as soon as a consistant state has been reached */
	worker.bgw_start_time = BgWorkerStart_ConsistentState;

	/* Set the name of the bgwriter library */
	snprintf(worker.bgw_library_name, BGW_MAXLEN, "cpg");

	/* Set the name of main function to start the bgworker */
	snprintf(worker.bgw_function_name, BGW_MAXLEN, "cpg_main");

	/* Set the name of the bgworker */
	snprintf(worker.bgw_name, BGW_MAXLEN, "cpg");

	/* For this demo, just never come back if the bgw exits */
	worker.bgw_restart_time = BGW_NEVER_RESTART;

	/* No arg to pass to the bgworker's main function */
	worker.bgw_main_arg = (Datum) 0;

	/* This bgworker is not loaded dynamicaly, no backend to notify after
	 * successful start */
	worker.bgw_notify_pid = 0;

	/*
	 * Register the bgworker with the postmaster
	 */
	RegisterBackgroundWorker(&worker);
}
